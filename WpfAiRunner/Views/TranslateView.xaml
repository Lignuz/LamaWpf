<local:BaseAiView x:Class="WpfAiRunner.Views.TranslateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WpfAiRunner.Views"
             Background="{StaticResource BgDark}">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource BgCard}" CornerRadius="8" Padding="10" Margin="0,0,0,15">
            <DockPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Source:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{StaticResource TextSecondary}"/>
                    <ComboBox x:Name="CboSourceLang" Width="120" Margin="0,0,20,0" SelectedIndex="0">
                        <ComboBoxItem Content="Auto Detect"/>
                        <ComboBoxItem Content="Korean"/>
                        <ComboBoxItem Content="English"/>
                    </ComboBox>

                    <TextBlock Text="Target:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{StaticResource TextSecondary}"/>
                    <ComboBox x:Name="CboTargetLang" Width="120" Margin="0,0,20,0" SelectedIndex="1">
                        <ComboBoxItem Content="Korean"/>
                        <ComboBoxItem Content="English"/>
                    </ComboBox>

                    <Button x:Name="BtnTranslate" Content="🌐 Translate Now" Style="{StaticResource BtnPrimary}" Click="BtnTranslate_Click" Width="140"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <ProgressBar x:Name="PbarLoading" Width="100" Height="4" IsIndeterminate="True" Visibility="Collapsed" Margin="0,0,10,0"/>
                    <TextBlock x:Name="TxtStatus" Text="Ready" VerticalAlignment="Center" Foreground="{StaticResource TextSecondary}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource BgCard}" CornerRadius="8" Margin="0,0,8,0" BorderThickness="1">
                <Grid>
                    <TextBox x:Name="TxtInput" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                             Background="Transparent" Foreground="{StaticResource TextPrimary}" BorderThickness="0" 
                             Padding="15" FontSize="14" Opacity="0.9"/>
                    <Button Content="Clear" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="5" Click="BtnClear_Click" Opacity="0.5" />
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="{StaticResource BgCard}" CornerRadius="8" Margin="8,0,0,0" BorderThickness="0,0,0,3">
                <Grid>
                    <TextBox x:Name="TxtOutput" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                             Background="Transparent" Foreground="{StaticResource TextPrimary}" BorderThickness="0" 
                             Padding="15" FontSize="14"/>
                    <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Right" Orientation="Horizontal" Margin="10">
                        <Button Content="📋 Copy" Click="BtnCopy_Click" Margin="5,0" Opacity="0.8"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</local:BaseAiView>